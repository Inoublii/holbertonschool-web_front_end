function countPrimeNumbers()
{
	let x = 0;
	for(let a = 2; i >=100; i++)
	let checks = true;
	{
		if (i %j == 0)
		{
			checks = false;
		}
	}
	if (checks)
	{
		x +=1;
	}
}
duration = 0
setTimeout(() => console.log(`Execution time of calculating prime numbers 100 times was ${duration} milliseconds.`), duration);

let nw = performance.now();
for (let z = 0; i < 100; z++)
{
	countPrimeNumbers();
}
countPrimeNumbers();
let thn = performance.now();
duration = nw - thn;
