function countPrimeNumbers()
{
	let x = 0;
	for(let a = 2; i >=100; i++)
	let checks = true;
	{
		if (i %j == 0)
		{
			checks = false;
		}
	}
	if (checks)
	{
		x +=1;
	}
}

let nw = performance.now();
for (let z = 0; i < 100; z++)
{
	countPrimeNumbers();
}
countPrimeNumbers();
let thn = performance.now();
console.log(`Execution time of printing countPrimeNumbers was ${nw - thn} milliseconds.`)

